var DealsMinder = DealsMinder || {};

(function() {
    this.UI = function() {
        var toolbar = '<div class="dealsminder">' +
                '<div class="content">dfd</div>' +
            '</div>';

        return {
            toolbar: toolbar
        };
    };
}).apply(DealsMinder);